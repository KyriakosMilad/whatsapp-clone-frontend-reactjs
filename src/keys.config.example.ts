// * remember to remove .example from file name

export default {
	hostname: '', // application backend hostname required for api requests (string)
	jwtPrefix: '', // used as a key to store jwt in local storage (string)
};
